# Dependencies
node_modules/

# Secrets — never commit real tokens or passwords
.env
.env.local
.env.*.local

# Vercel
.vercel/

# OS / editor cruft
.DS_Store
Thumbs.db

# Local Claude Code settings (machine-specific; may contain command history)
.claude/settings.local.json
