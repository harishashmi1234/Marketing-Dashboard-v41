{
  "name": "gsl-marketing-mirror",
  "version": "1.0.0",
  "private": true,
  "description": "GSL Marketing Mirror — marketing dashboard with live Meta Ads data via a Vercel serverless proxy.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "node server.js",
    "dev:vercel": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@vercel/functions": "^3.7.1"
  }
}
